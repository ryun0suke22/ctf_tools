version: '3'
services:
  kali-linux:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: ctf-kali
    volumes:
      - /Users/r.murakami/Works:/root/Works
    ports:
      - 20021:20022
    env_file:
      - .env
    tty: true
    stdin_open: true
    command: >
      /bin/zsh
